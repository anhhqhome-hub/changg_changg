// SQLite demo template stored as JSON to avoid a thousands-deep binary-expression AST.
import embeddedDemoDb from "@/lib/embedded-demo-db.json";

export const EMBEDDED_DEMO_DB_BASE64 = embeddedDemoDb.base64;
